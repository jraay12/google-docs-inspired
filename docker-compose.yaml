services:

  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: google-docs-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    depends_on:
      db:
        condition: service_healthy
  db:
    image: mysql:8.0
    container_name: google-docs-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: qwerty
      MYSQL_DATABASE: google-docs-inspired
    ports:
      - "3306:3306"
    volumes:
      - google-docs-inspired-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pqwerty"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: google-docs-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: "qwerty"
    depends_on:
      db:
        condition: service_healthy

volumes:
  google-docs-inspired-data: